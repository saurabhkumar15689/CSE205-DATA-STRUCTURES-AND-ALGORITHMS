# Binary to Decimal

#include <iostream>
#include <cmath>
using namespace std;

int main(){
    int n,i=0,dec=0,r;
    cin>>n;
    while(n>0){
        r=n%10;
        dec+=r*pow(2,i);
        n/=10;
        i++;
    }
    cout<<dec;
}
